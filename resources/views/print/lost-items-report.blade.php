<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Lost and Found Report - {{ $resort->name }}</title>
    <style>
        body {
            font-family: DejaVu Sans, sans-serif;
            margin: 30px;
            color: #333;
        }

        .logo {
            text-align: center;
            margin-bottom: -80px;
            margin-top: -80px;
        }

        .logo img {
            max-height: 200px;
        }

        h3 {
            text-align: center;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
            margin-top: 0;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 25px;
        }

        th,
        td {
            border: 1px solid #aaa;
            padding: 8px;
            text-align: left;
            vertical-align: top;
        }

        th {
            background-color: #f2f2f2;
        }

        .totals {
            text-align: left;
            margin-top: 0;
            font-size: 10px;
            font-weight: bold;
        }

        .footer {
            text-align: center;
            margin-top: 50px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="logo">
        <img src="{{ public_path('img/logo1.png') }}" alt="Logo">
    </div>

    <h3>{{ $resort->name }} — Lost and Found Report</h3>

    <p><strong>Date Generated:</strong> {{ now()->format('F d, Y') }}</p>

    <table>
        <thead style="font-size: 12px;">
            <tr>
                <th>#</th>
                <th>Reported By</th>
                <th>Description</th>
                <th>Date</th>
                <th>Location</th>
                <th>Status</th>
                <th>Remarks</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody style="font-size: 12px;">
            @forelse ($lostItems as $index => $item)
                <tr>
                    <td>{{ $index + 1 }}</td>
                    <td>{{ $item->user?->name ?? 'Guest' }}</td>
                    <td>{{ $item->description ?? 'N/A' }}</td>
                    <td>{{ \Carbon\Carbon::parse($item->date)->format('M d, Y') ?? 'Unknown' }}</td>
                    <td>{{ $item->location ?? 'N/A' }}</td>
                    <td>{{ ucfirst($item->status ?? 'Unspecified') }}</td>
                    <td>{{ $item->remarks ?? 'None' }}</td>
                    <td>{{ ucfirst($item->type ?? 'Unspecified') }}</td>
                </tr>
            @empty
                <tr>
                    <td colspan="8" style="text-align:center;">No lost or found items recorded.</td>
                </tr>
            @endforelse
        </tbody>
    </table>

    @php
        $totalLost = $lostItems->where('type', 'lost_item')->count();
        $totalFound = $lostItems->where('type', 'found_item')->count();
        $overallTotal = $lostItems->count();
    @endphp

    <div class="totals align-left">
        <span style="font-size: 14px;">Summary</span><br>
        <span>Total Lost Items: {{ $totalLost }}</span><br>
        <span>Total Found Items: {{ $totalFound }}</span><br>
        <span>Overall Total: {{ $overallTotal }}</span>
    </div>

    <div class="footer">
        Generated by {{ auth()->user()->name ?? 'System' }} | {{ now()->format('M d, Y h:i A') }}
    </div>
</body>

</html>
